{
  "name": "mf-menu",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@material-ui/icons": "^4.9.1",
    "@material-ui/lab": "^4.0.0-alpha.55",
    "@testing-library/jest-dom": "^4.2.4",
    "@testing-library/react": "^9.3.2",
    "@testing-library/user-event": "^7.1.2",
    "react": "^16.13.1",
    "react-dom": "^16.13.1",
    "react-promise-suspense": "^0.3.3"
  },
  "scripts": {
    "watch": "nodemon --delay 5 --watch src --watch public --watch index.js --exec \"yarn build && serve -s build --cors -p 4000\"",
    "start": "PORT=4000 NODE_ENV=production react-app-rewired start",
    "build": "ADDPATH=/MenuMF react-app-rewired build",
    "test": "react-app-rewired test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "@material-ui/core": "^4.10.1",
    "nodemon": "^2.0.4",
    "react-app-rewired": "^2.1.6",
    "react-scripts": "3.4.1",
    "webpack-visualizer-plugin": "^0.1.11"
  }
}
